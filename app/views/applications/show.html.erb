<div class="app_details">
	<%= image_tag @application.logo.url(:big), :class => "app_logo",
		:alt => @application.name, :title => @application.name %>
		
	<p><b>Aplicaci&oacute;n:</b> <%=h @application.name %></p>
	<p><b>Tags:</b> <%=h @application.tag_list %></p>
	<p><b>P&aacute;gina web:</b> <%= link_to @application.website.split(/\/\//)[1], @application.website unless @application.website.nil? %></p>
	<p><b>Recomendada por:</b> <%= @application.user_recommend.name if @application.user_recommend %>Ofincia de Software Libre</p>
	<p><b>Descripci&oacute;n:</b> <%=h @application.description %></p>	

	<% if !@application.installers.empty? %>
		<%= render :partial => '/shared/installers_list' %>
	<% end %>
	
</div>
<div id="app_details_bottom">&nbsp;</div>

<% if logged_in? and current_user.has_role?('administrator') %>
	<%= link_to 'Edit', edit_application_path(@tags, @application.to_param) %> | 
	<% #= link_to 'Screenshots', application_screenshots_path(@application) %> | 
	<% #= link_to 'Resources', application_resources_path(@application) %> |
	<% #= link_to 'Installers', application_installers_path(@application) %>
<% end %>

