<h4><%= link_to 'Categor&iacute;as', categories_path %> > <%= link_to @category.name, category_applications_path %></h4>

<% for application in @applications %>
	<p class="application">
		<%= link_to (image_tag application.logo.url, :class => "app_logo", :alt => application.name,
					:title => application.name), [@category, application] %>
		<%= link_to application.name, [@category, application] %> 
		<%=h application.description[0,260] %>
		<%=h '...' if application.description.length > 260 %><br />
	</p>

    <% #= link_to 'Show', [@category, application] %>
    <% #= link_to 'Edit', [:edit, @category, application] %>
    <% #= link_to 'Destroy', [@category, application], :confirm => 'Are you sure?', :method => :delete %>

<% end %>

<br />
<% if logged_in? and current_user.has_role?('administrator') %>
	<%= link_to 'New application', new_category_application_path(@category) %>
<% end %>