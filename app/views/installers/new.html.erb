<h3>New <%= @application.name %> Installer</h3>

<%= render :partial => @installer, :locals => { :button_name => "Create" } %>

<p><%= link_to "Back to list", application_installers_path(@tags, @application) %></p>
